[
  {
    "description": "Flags Open/Closed violation — strategy pattern opportunity",
    "matchLineRange": [4, 33],
    "matchRecommendationContainsAny": ["strategy", "handler", "Open/Closed", "map of", "switch", "dispatch", "polymorphi", "lookup", "Record", "extract"],
    "required": true
  },
  {
    "description": "Flags SRP violation — each case does too much",
    "matchLineRange": [4, 33],
    "matchRecommendationContainsAny": ["dedicated", "separate", "extract", "handler"],
    "required": false
  }
]
