[
  {
    "description": "Flags god component with multiple responsibilities",
    "matchLineRange": [1, 9],
    "matchRecommendationContainsAny": ["extract", "custom hook", "split", "separate"],
    "required": true
  },
  {
    "description": "Flags inline data filtering/sorting",
    "matchLineRange": [5, 8],
    "matchRecommendationContainsAny": ["extract", "useMemo", "memoiz"],
    "required": false
  }
]
