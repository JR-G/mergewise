[
  {
    "description": "Flags unstable inline object as context provider value",
    "matchLineRange": [20, 26],
    "matchRecommendationContainsAny": ["useMemo", "memo", "stable", "object"],
    "required": true
  },
  {
    "description": "Flags mixed validation logic in component",
    "matchLineRange": [9, 20],
    "matchRecommendationContainsAny": ["extract", "validation", "separate", "helper"],
    "required": true
  }
]
