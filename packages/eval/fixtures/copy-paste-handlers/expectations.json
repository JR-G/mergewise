[
  {
    "description": "Flags DRY violation — duplicated validation logic",
    "matchLineRange": [2, 30],
    "matchRecommendationContainsAny": ["extract", "shared", "common", "duplicat"],
    "required": true
  },
  {
    "description": "Flags vague naming — handleData/processResult",
    "matchLineRange": [16, 36],
    "matchRecommendationContainsAny": ["simplify", "rename", "inline", "name"],
    "required": true
  }
]
