[
  {
    "description": "Should not flag properly structured component as god-component",
    "matchLineRange": [1, 77],
    "matchRecommendationContainsAny": ["god", "too many responsibilities", "split"],
    "required": false,
    "forbidden": true
  },
  {
    "description": "Should not flag correct useMemo/useCallback usage as unnecessary",
    "matchLineRange": [1, 77],
    "matchRecommendationContainsAny": ["unnecessary", "remove useMemo", "remove useCallback", "does not depend", "not needed", "overkill", "trivial"],
    "required": false,
    "forbidden": true
  },
  {
    "description": "Should not flag idiomatic type narrowing in select handler",
    "matchLineRange": [35, 77],
    "matchRecommendationContainsAny": ["type safety", "cast", "assertion"],
    "required": false,
    "forbidden": true
  },
  {
    "description": "Should not flag useMemo with filter+sort as a performance issue when deps are correct",
    "matchLineRange": [27, 31],
    "matchRecommendationContainsAny": ["every render", "performance", "re-render", "expensive"],
    "required": false,
    "forbidden": true
  },
  {
    "description": "Should not flag handleRoleChange mixing onRoleChange with setIsEditing as SRP violation",
    "matchLineRange": [32, 38],
    "matchRecommendationContainsAny": ["mixes", "responsibilit", "separate concern"],
    "required": false,
    "forbidden": true
  }
]
