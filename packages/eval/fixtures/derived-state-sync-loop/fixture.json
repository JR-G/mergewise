{
  "executionMode": "pipeline",
  "prTitle": "Add filterable list UI",
  "prDescription": "Stores filtered items in state and sorts them during render.",
  "reviewQuality": {
    "summary": "The diff contains one dominant issue and one secondary issue: derived state synchronisation plus unmemoised sorting in render.",
    "reviewGoal": "A strong reviewer must catch the derive-don't-sync issue and may optionally also flag the sorting work, with the derived-state concern treated as the more important one.",
    "mustFind": [
      {
        "description": "Flags the derived state sync loop",
        "matchLineRange": [4, 9],
        "matchRecommendationContainsAny": ["derive", "useMemo", "inline", "compute"],
        "required": true
      },
      {
        "description": "Flags the sorting work in render",
        "matchLineRange": [9, 11],
        "matchRecommendationContainsAny": ["useMemo", "memo", "sort"],
        "required": false
      }
    ],
    "mustAvoid": [],
    "findingCountRange": [1, 2],
    "prioritise": [
      {
        "description": "The first comment should be the derived state issue",
        "matchLineRange": [4, 9],
        "matchRecommendationContainsAny": ["derive", "useMemo", "inline", "compute"],
        "required": true
      }
    ],
    "dimensions": [
      { "name": "prioritisation", "description": "Did the review understand that derived state is the bigger issue here?" },
      { "name": "specificity", "description": "Did it distinguish the sync-loop problem from the sorting concern clearly?" }
    ]
  }
}
